{
  "name": "menu-interactivo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Portafolio personal con menú estilo videojuego: pasillo 3D en CSS y una habitación por sección.",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node serve.mjs",
    "start": "node build.mjs && node serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
